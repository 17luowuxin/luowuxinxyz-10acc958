# 梦境小手机 - 阿里云部署

## 部署步骤

1. SSH到服务器
2. cd /www/wwwroot/louwuxin.xyz
3. git pull origin main
4. npm install
5. npm run build
6. /etc/init.d/nginx restart

## 更新流程

```bash
cd /www/wwwroot/louwuxin.xyz && git pull origin main && npm install && npm run build
```

## 目录结构

- /dist - 构建产物（Nginx直接serve）
- /ai/ - AI工具静态HTML
- /ai-server/ - API代理（端口3000，systemd服务ai-proxy）
